test_that("Estructura base de datos", {

  # bd_analizar_morena <- analizar_morena(diseno = survey::svydesign(ids = ~1, data = datos_demo),
  #                                       diccionario = cuestionario_demo, personajes = c("era", "sasil"),
  #                                       atributos = tibble(atributo = c("honesto", "mujeres", "cercano", "estado", "cumple"),
  #                                                          puntos = c(1,.5,.25,.25,.25)))
  # # Estrcutura de la base de datos
  # testthat::expect_equal(names(bd_analizar_frecuencias), c("respuesta", "media", "ee", "pregunta"))

})
